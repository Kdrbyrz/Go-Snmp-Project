please change URL on this parts:
resp, err := http.NewRequest("POST","URL",  strings.NewReader(string(bytes)) )

A project for getting information from your system via snmp and send it with http request.
